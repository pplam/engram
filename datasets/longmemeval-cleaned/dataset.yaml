# LongMemEval-S Cleaned (D1): 500 questions, one ~53-session haystack each.
#
# LICENCE: MIT — no publication restriction, unlike LoCoMo-Refined.
#
# No `official_judge` or `official_prompt`: unlike LoCoMo-Refined, this dataset does
# not define its own scoring, so the suite's judge and prompt apply. That absence is
# deliberate and is what D7's precedence rule resolves — the resolved judge still
# lands in the manifest and on the row, labelled `suite` rather than `dataset`.
name: longmemeval-cleaned
version: "1.0"
source:
  path: data/longmemeval_s_cleaned.json
  sha256: d6f21ea9d60a0d56f34a05b609c79c88a451d2ae03597821ea3d5a9678c3a442
  url: https://huggingface.co/datasets/xiaowu0162/longmemeval-cleaned/resolve/main/longmemeval_s_cleaned.json
task_type: open_qa
scorer: judge_binary
supports_recall: true
# `answer_session_ids` names whole sessions, so a gold session expands to every chunk
# derived from it. Coarser than LoCoMo's message unit, hence not comparable across the two.
evidence_granularity: session
